'use strict'

// BUCLE FOR
// BUCLE es una estructura de control que se repite varias veces 

var numero = 100;

// se define variable var i = 0; 
// se condiciona hasta que caso va seguir funcionando el bucle(al valor 100 se detiene el bucle; 
// se incrementa ++/decrementa -- contador)
for(var i = 0; i <= numero; i++){  
    console.log("vamos en el numero "+i);  
    
    // debugger; permite ver lo que pasa dentro del bloque mediante el debugger del navegador
}